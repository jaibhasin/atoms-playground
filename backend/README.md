# Atoms API Backend

Simple Express API serving atomic data for the visualization frontend.

## Endpoints

- `GET /api/atoms` - Get all atoms
- `GET /api/atoms/:symbol` - Get specific atom by symbol
- `GET /api/health` - Health check

## Development

```bash
npm install
npm run dev
```

Server runs on http://localhost:3001
